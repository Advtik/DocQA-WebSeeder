# DocQA Frontend

Minimal React/Vite UI for uploading a PDF, asking a question, and viewing the answer.

## Run

```powershell
cd "C:\Dev Work\DocQA\frontend"
npm install
npm run dev
```

Open `http://localhost:5173`.

The backend must be running at `http://localhost:8000`.
